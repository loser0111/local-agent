# LeetCode 每日一题：2472. Maximum Number of Non-overlapping Palindrome Substrings

- 日期：2026-09-15
- 题号：2472
- 难度：Hard（困难）
- 通过率：55.92%
- 标签：Two Pointers, String, Dynamic Programming, Greedy
- 链接：https://leetcode.com/problems/maximum-number-of-non-overlapping-palindrome-substrings/

---

## 一、题目描述

给你一个字符串 s 和一个正整数 k。
从字符串 s 中选择一组 **互不重叠** 的子字符串，要求同时满足：

1. 每个子字符串的长度 **至少为 k**；
2. 每个子字符串都必须是 **回文串**。

返回这样的集合中，最多能包含的子字符串数量。

> 子字符串指字符串中连续的一段字符。

## 二、示例

**示例 1**

    输入：s = "abaccdbbd", k = 3
    输出：2
    解释：可以选择 "aba" 和 "dbbd" 两个回文子串，共 2 个。可以证明 2 是最大值。

**示例 2**

    输入：s = "adbcda", k = 2
    输出：0
    解释：不存在长度至少为 2 的回文子串，返回 0。

---

## 三、数据范围

- 1 <= k <= s.length <= 2000
- s 仅由小写英文字母组成

---

## 四、解题思路

### 观察 1：只需要考虑长度为 k 或 k+1 的回文串

任何长度 L >= k 的回文串，都关于某个中心对称。取出与它 **同中心**、长度恰好为 k 或 k+1 的那一小段，它同样是回文串：

- 若 k 为偶数：偶数长度的中心给出长度 k 的回文，奇数长度的中心给出长度 k+1 的回文；
- 若 k 为奇数：奇数长度的中心给出长度 k 的回文，偶数长度的中心给出长度 k+1 的回文。

因为这一小段被原回文串完全包含，替换后不会与其它已选区间产生新的重叠，却占用更少的字符，所以 **一定存在一个最优解，其中所有回文子串的长度都恰好是 k 或 k+1**。

### 观察 2：转化为区间调度问题

于是问题变成：在字符串中选出尽可能多的、互不重叠的区间，每个区间对应一个长度为 k 或 k+1 的回文子串。这是经典的 **区间调度** 问题，最优策略是 **贪心：每次选择结束位置最早的区间**。

### 观察 3：从左往右扫描即可

由于候选区间的长度只有 k 或 k+1，起始位置越靠前，结束位置也越靠前（或相等）。因此 **从左到右扫描，遇到第一个可用的回文区间就选它**，等价于选择结束最早的区间的贪心。

### 贪心流程

令 i 从 0 开始向后扫描：

- 若 s[i .. i+k-1] 是回文（长度 k），选它，答案加 1，i 跳到 i+k；
- 否则若 s[i .. i+k] 是回文（长度 k+1），选它，答案加 1，i 跳到 i+k+1；
- 否则 i 加 1，继续。

优先尝试长度 k（更短、更早结束），其次 k+1。

---

## 五、代码实现

### 方法一：贪心 + 回文判断（推荐，额外空间 O(1)）

    class Solution:
        def maxPalindromes(self, s: str, k: int) -> int:
            n = len(s)
            ans = 0
            i = 0
            while i < n:
                # 优先选长度 k（更短，更早结束）
                if i + k <= n and self.isPalindrome(s, i, i + k - 1):
                    ans += 1
                    i += k
                # 其次选长度 k + 1
                elif i + k + 1 <= n and self.isPalindrome(s, i, i + k):
                    ans += 1
                    i += k + 1
                else:
                    i += 1
            return ans

        def isPalindrome(self, s: str, left: int, right: int) -> bool:
            while left < right:
                if s[left] != s[right]:
                    return False
                left += 1
                right -= 1
            return True

### 方法二：动态规划

设 dp[i] 表示只使用 s 的前 i 个字符时，最多能选出的回文子串数量。

- 不选以位置 i-1 结尾的子串：dp[i] = dp[i-1]；
- 选区间 [j, i-1]（长度不小于 k 且为回文）：dp[i] = max(dp[i], dp[j] + 1)。

先用 pal[i][j] 预处理任意子串是否为回文。

    class Solution:
        def maxPalindromes(self, s: str, k: int) -> int:
            n = len(s)
            # pal[i][j] 表示 s[i..j] 是否为回文串
            pal = [[False] * n for _ in range(n)]
            for i in range(n - 1, -1, -1):
                pal[i][i] = True
                for j in range(i + 1, n):
                    if s[i] == s[j]:
                        pal[i][j] = (j - i < 2) or pal[i + 1][j - 1]

            dp = [0] * (n + 1)  # dp[i]：前 i 个字符的最优解
            for i in range(1, n + 1):
                dp[i] = dp[i - 1]  # 不以 i-1 结尾选子串
                for j in range(0, i - k + 1):
                    if pal[j][i - 1]:
                        dp[i] = max(dp[i], dp[j] + 1)
            return dp[n]

---

## 六、示例走查（s = abaccdbbd, k = 3）

k = 3 为奇数，因此候选长度为 3 和 4。

- i = 0：s[0..2] = aba 是回文，选它，ans = 1，i 跳到 3；
- i = 3：s[3..5] = ccd 不是回文；s[3..6] = ccdb 不是回文，i 加 1 到 4；
- i = 4：s[4..6] = cdb 不是回文；s[4..7] = cdbb 不是回文，i 加 1 到 5；
- i = 5：s[5..7] = dbb 不是回文；s[5..8] = dbbd 是回文，选它，ans = 2，i 跳到 9；
- i = 9 越界，结束。

最终答案为 2，与示例一致。

---

## 七、复杂度分析

| 方法 | 时间复杂度 | 空间复杂度 |
| --- | --- | --- |
| 贪心 + 回文判断 | O(n^2) | O(1) |
| 动态规划 | O(n^2) | O(n^2) |

其中 n 为字符串长度。贪心方法中每次回文判断最多 O(n)，共 O(n) 次，因此整体为 O(n^2)。

---

## 八、小结

- 关键：**任意长回文都可以退化为同中心的、长度为 k 或 k+1 的回文**，从而把问题简化为区间调度。
- 贪心选择结束最早的区间即为最优。
- 由于候选长度只有 k 与 k+1，从左往右遇到即可取。
